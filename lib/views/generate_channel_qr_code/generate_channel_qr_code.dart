import 'package:flutter/material.dart';

class GenerateChannelQrCode extends StatelessWidget {
  const GenerateChannelQrCode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
